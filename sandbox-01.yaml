name: "sandbox-basic"
spec:
  description: sandbox with no changes
  cluster: "@{cluster}"
  defaultRouteGroup:
    endpoints:
    - name: frontend-endpoint
      target: http://frontend.hotrod.svc:8080
